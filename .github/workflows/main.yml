name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist"

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

---

### Step 3: Name the File

At the top, you'll see the filename. Make sure it says:
```
.github/workflows/deploy.yml
```

---

### Step 4: Commit the File

1. Click the green **"Commit changes..."** button (top right)
2. In the popup, click **"Commit changes"**

---

### Step 5: Enable GitHub Pages

1. Go to **Settings** tab (top menu)
2. Click **Pages** (left sidebar)
3. Under "Build and deployment" → Source: Select **"GitHub Actions"**

---

### Step 6: Watch It Deploy

1. Go back to **Actions** tab
2. You should see your workflow running 🟡
3. Wait 2-3 minutes until it turns ✅ green

---

## 📷 Visual Guide
```
┌────────────────────────────────────────────────────┐
│                                                    │
│  1. Click "set up a workflow yourself"             │
│                                                    │
│  2. Paste the YAML code above                      │
│                                                    │
│  3. Filename: .github/workflows/deploy.yml         │
│                                                    │
│  4. Click [Commit changes...]                      │
│                                                    │
│  5. Settings → Pages → Source: GitHub Actions      │
│                                                    │
│  6. Actions tab → Watch it build ✅                │
│                                                    │
└────────────────────────────────────────────────────┘
```

---

After it's done, your site will be at:
```
https://naga508.github.io/ai-coaching-agent/
